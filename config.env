#!/bin/bash
# CCDC Environment Configuration
# ================================
# Fill in at competition start. Only the Splunk fields are required by scripts.
# Everything else is for your team's reference (print QUICKREF.md for the full table).
#
# After filling in values, set CONFIGURED=true at the bottom.

#=============================================================================
# Splunk (required by 04-splunk.sh)
#=============================================================================
export SPLUNK_SERVER=""          # IP of your Splunk server (from competition packet)
export SPLUNK_PORT="9997"       # Usually 9997 -- don't change unless told to
export SPLUNK_VERSION=""        # Check Splunk server: /opt/splunk/bin/splunk version
export SPLUNK_BUILD=""          # Same command shows the build hash

#=============================================================================
# Competition User (used by 01-harden.sh to create a sudo account)
#=============================================================================
export COMP_USER="sysadmin"     # Name of the sudo user created on all Linux hosts

#=============================================================================
# IMPORTANT: Set to true after filling in Splunk values
#=============================================================================
export CONFIGURED="false"
